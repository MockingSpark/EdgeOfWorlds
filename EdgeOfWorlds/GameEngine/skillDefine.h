#pragma once

enum SkillRange{
	R_AREA,
	R_TEAM,
	R_OTHERS,
	R_ALL
};

enum TargetTeam {
	T_TEAM,
	T_ENEMY,
	T_BOTH,
	T_SELF,
	T_STAGE
};

enum EdgeSide {
	S_MEDIEVAL,
	S_TECH,
	S_BOTH,
	S_EITHER,
	S_SAME,
	S_OTHER
};